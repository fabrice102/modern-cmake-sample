language: cpp

matrix:
  include:
    - os: linux
      dist: xenial
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-7 rapidjson-dev libboost-regex-dev
      env:
        - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7 && BUILD_TYPE=Debug"
    - os: linux
      dist: xenial
      addons:
        apt:
          sources:
            - llvm-toolchain-xenial-6.0
          packages:
            - clang-6.0 rapidjson-dev libboost-regex-dev
      env:
        - MATRIX_EVAL="CC=clang-6.0 && CXX=clang++-6.0 && BUILD_TYPE=Debug"
    - os: osx
      osx_image: xcode10.1
      env:
        - MATRIX_EVAL="BUILD_TYPE=Debug"
      addons:
        homebrew:
          packages:
            - boost

before_install:
  - eval "${MATRIX_EVAL}"

script: cd libjsonutils && mkdir cmake-build && cd cmake-build && cmake -DCMAKE_BUILD_TYPE="${BUILD_TYPE}" .. && make && make test
